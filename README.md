# bookmark-path-finder
A client-side enumeration bookmarklet that fetches and parses loaded resources and page markup to discover referenced relative paths and API routes. Results are deduplicated, categorized, and rendered in a searchable interface to accelerate manual analysis during web application security assessments.
